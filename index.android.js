import React, { Component } from 'react';
import { AppRegistry } from 'react-native';

import App from './App';

class artproject extends Component {
  render() {
    return <App />;
  }
}

AppRegistry.registerComponent('artproject', () => artproject);
